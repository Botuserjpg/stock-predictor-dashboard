# Stock Predictor Pro - runtime dependencies.
#
# Pinned to the versions actually installed and tested in .venv310
# (Python 3.10.11) so `pip install -r requirements.txt` reproduces the
# environment the suite runs green in.
#
# Optional - only imported inside guarded try/except blocks, may be omitted:
#   openai, anthropic (LLM report), shap (explainability), psutil (run_app.py),
#   fbprophet (legacy import alias of prophet in model_utils.py).
#
# Development tools live in requirements-dev.txt.
# ML/research-only extras (streamlit dashboards, prophet, pmdarima, plotly)
# stay listed here because predict.py / model_utils.py import them at module
# scope in some code paths; splitting them out is backlog ticket H-06 follow-up.

# Core web runtime
Flask==3.1.3
Flask-Login==0.6.3
Flask-SocketIO==5.6.1
Werkzeug==3.1.8
MarkupSafe==3.0.2
itsdangerous==2.2.0
python-dotenv==1.2.2
SQLAlchemy==2.0.51
alembic==1.19.1
APScheduler==3.11.3

# Production serving (Docker CMD: gunicorn + eventlet worker for Socket.IO)
gunicorn==23.0.0
eventlet==0.36.1

# Data & ML runtime
numpy==1.26.4
pandas==2.3.3
scipy==1.14.1
matplotlib==3.7.1
plotly==6.2.0
statsmodels==0.14.4
pmdarima==2.0.4
prophet==1.1.7
scikit-learn==1.7.2
seaborn==0.13.2
textblob==0.19.0
streamlit==1.47.0

# Market data & HTTP
yfinance==1.5.2
requests==2.34.2
aiohttp==3.12.15
beautifulsoup4==4.13.3

tensorflow==2.15.0
resend